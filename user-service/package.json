{
  "name": "user-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "if -d build; then rm -rf build; fi",
    "build": "npm run clean && tsc -p .",
    "local": "npm run build && node build/index",
    "docker": "node build/index"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.7",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "typescript": "3.9.6"
  }
}
